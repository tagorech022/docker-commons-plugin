<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.jenkins.plugins</groupId>
        <artifactId>plugin</artifactId>
        <version>4.61</version>
        <relativePath/> <!-- Don't try to resolve from local parent -->
    </parent>

    <groupId>org.jenkinsci.plugins</groupId>
    <artifactId>docker-commons-sample</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>hpi</packaging>

    <name>Docker Commons Sample Plugin</name>
    <description>Sample Jenkins plugin demonstrating Docker server and registry endpoint usage.</description>
    <url>https://github.com/your-org/docker-commons-sample</url>

    <properties>
        <java.level>11</java.level>
        <jenkins.version>2.387.1</jenkins.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.jenkins-ci.plugins</groupId>
            <artifactId>docker-commons</artifactId>
            <version>1.21</version>
        </dependency>
        <dependency>
            <groupId>org.jenkins-ci</groupId>
            <artifactId>plugin</artifactId>
            <version>3.58</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <repositories>
        <repository>
            <id>jenkins</id>
            <url>https://repo.jenkins-ci.org/public/</url>
        </repository>
    </repositories>

</project>
